name: Check Code

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Kotlin Linter Check
        uses: vroy/gha-kotlin-linter@v2
